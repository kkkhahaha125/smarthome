# ===================== 项目特有忽略项（核心） =====================
# 编译产物目录（obj下是.o/.out/可执行文件，无需上传）
obj/
# VSCode本地配置（个性化设置，团队无需同步）
.vscode/
# 第三方依赖目录（3rd通常是外部库，可通过文档说明安装，无需上传）
#3rd/

# ===================== C/C++通用忽略项 =====================
# 目标文件（编译生成的.o文件）
*.o
# 静态库
*.a
# 动态库
*.so
# 可执行文件（根据你的项目名补充，比如smarthome主程序）
smarthome
# 核心转储文件（程序崩溃生成的core文件）
core
core.*

# ===================== 通用临时/备份文件 =====================
# Vim交换文件（编辑时生成的临时文件）
*.swp
# 编辑器备份文件
*~
# 日志文件
*.log
# 备份文件
*.bak
*.orig
# Mac系统隐藏文件（若从Mac传文件到虚拟机）
.DS_Store
